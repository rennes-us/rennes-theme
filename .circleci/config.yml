version: 2.1
orbs:
  python: circleci/python@0.2.1
jobs:
  build:
    executor: python/default
    steps:
      - checkout
      - run:
          command: echo hi
          name: Test
workflows:
  main:
    jobs:
      - build
